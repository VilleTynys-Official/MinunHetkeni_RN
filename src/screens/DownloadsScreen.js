import React from 'react';
import { View, Text, StyleSheet, Button } from 'react-native';

//tämä on toistaiseksi vain scaffold sivulle johon tallentuisivat kategoriat, jotka käyttäjä no tallentanut offline tilassa käytettäväksi.

const DownloadsScreen = () => {
    return (
        <>
            <Text style={StyleSheet.create({ fontSize: 50 })} >DownloadsScreen</Text>
        </>
    )
};


const styles = StyleSheet.create({});


export default DownloadsScreen;